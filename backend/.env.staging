# Staging Environment Variables
# Server Configuration
PORT=5000
NODE_ENV=staging
FRONTEND_URL=https://skillgrid-staging.vercel.app

# Email Configuration (Set these in your deployment platform)
EMAIL_USER=skillgrit3@gmail.com
EMAIL_PASS=your-gmail-app-password-here

# Security (Generate a strong JWT secret for staging)
JWT_SECRET=skillgrid-staging-jwt-secret-2024